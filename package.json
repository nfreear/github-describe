{
  "publish": false,
  "name": "github-describe",
  "version": "1.0.0",
  "description": "Run 'git describe' on a GitHub repository, in the browser. Useful for GitHub pages sites!",
  "license": "UNLICENSED",
  "main": "index.js",
  "author": "© Nick Freear, 05-July-2018.",
  "repository": "https://github.com/nfreear/github-describe.git",
  "bugs": "https://github.com/nfreear/github-describe/issues",
  "engines": { "node": ">= 6" },
  "dependencies": {
    "superagent": "^3.8.3"
  },
  "peerDependencies": {
    "jquery": "^3.3.1"
  },
  "devDependencies": {
    "browserify": "^16.2.2",
    "replace": "^1.0.0",
    "semistandard": "^12.0.1",
    "uglify-js": "^3.4.3"
  },
  "keywords": [ "git-describe", "github", "git", "github-pages" ],
  "scripts": {
    "build": "browserify index.js > dist/dist.js && bin/_rep.js && npm run uglify",
    "uglify": "uglifyjs dist/dist.js --comments=/^[!]/ -mco dist/min.js",
    "fix": "semistandard --fix",
    "test": "semistandard"
  }
}
